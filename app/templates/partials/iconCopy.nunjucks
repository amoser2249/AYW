
{% for item in iconCopy %}
  <div class="iconCopy__item">
  <div class="iconCopy__visual">
    <img src="{{item.visual}}">
  </div>
  <h4>{{item.headline}}</h4>
  <p>{{item.body | truncate(160)}}</p>
  </div>
{% endfor %}
